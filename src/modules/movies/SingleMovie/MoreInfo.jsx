import React from 'react';
import { MoreInfoContainer, MovieDataKey, MovieDataValue } from './styles';


export default ({ className }) => (
  <MoreInfoContainer className={className}>
  </MoreInfoContainer>
);
